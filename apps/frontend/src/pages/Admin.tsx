import { Box, Heading, Text } from "@chakra-ui/react";

const AdminPanel = () => {
  return (
    <Box>
      <Heading>Admin Panel</Heading>
      <Text mt={4}>Manage roles, users, and audit logs here.</Text>
    </Box>
  );
};

export default AdminPanel;
