# Git files
.git
.gitignore

# GitHub specific
.github/

# IDE and editor files
.idea/
.vscode/
*.sublime-project
*.sublime-workspace

# Node.js dependencies (let Docker handle these based on package.json)
**/node_modules
node_modules/

# Log files
*.log
logs/
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*
lerna-debug.log*

# Build outputs (these are usually rebuilt inside Docker or copied from builder stage)
**/dist
**/build
**/out
.next/
.nuxt/
.svelte-kit/

# Test and coverage reports
coverage/
*.lcov
junit.xml
TESTS-*.xml

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Environment files (should use Docker's env mechanisms)
.env
.env.*
!.env.example # Keep example files in context if needed for scripts

# Docker files themselves (not needed inside the image)
Dockerfile
docker-compose.yml
docker-compose.*.yml
.dockerignore # This file itself

# Terraform state and configuration (not part of application image)
terraform/
.terraform/
*.tfstate
*.tfstate.backup
*.tfvars
*.tfplan
terraform.tfvars

# Temporary files and caches
*.tmp
*.bak
*.swp
.cache/
cache/

# Specific project/tool caches or outputs
.eslintcache
.prettiercache
.stylelintcache
.turbo/

# Sensitive files
*.pem
*.key
*.cer
*.p12
# Add any other sensitive file patterns

# Monorepo specific: if some packages are purely for local dev scripts and not needed in any image
# e.g. packages/some-dev-tool/
```

I made this `.dockerignore` more comprehensive for monorepo setups.
