# Environment variables for backend-api service
NODE_ENV=development
API_PORT=3000

# Database (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_USER=admin
DB_PASSWORD=secret
DB_NAME=support_dashboard_db
TYPEORM_SYNCHRONIZE=false # Set to true ONLY for local development if you want to auto-sync schema (USE MIGRATIONS FOR PROD/STAGING)

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# JWT
JWT_SECRET="your-backend-specific-super-secret-jwt-token" # Can override root .env
JWT_REFRESH_SECRET="your-backend-specific-super-secret-jwt-refresh-token" # Can override root .env
JWT_ACCESS_TOKEN_EXPIRATION="15m"
JWT_REFRESH_TOKEN_EXPIRATION="7d"

# Frontend URL (for CORS)
FRONTEND_URL="http://localhost:3001" # Port of the frontend dev server

# OpenAI / LLM Provider (can be inherited from root .env or overridden)
# OPENAI_API_KEY=
# LLM_PROVIDER=openai
# LOCAL_LLM_URL=

LOG_LEVEL=debug
```

**7. Add an `.eslintignore` specific to backend (if needed, though root might cover it):**
The root `.eslintignore` should already cover `dist` and `node_modules`.

**8. Add a basic `.eslintrc.js` specific to backend (optional, can extend root):**
```typescript
