import React from 'react';
import ReactDOM from 'react-dom/client';

// Placeholder for App component
const App = () => (
  <div>
    <h1>Real-Time AI Support Dashboard</h1>
    <p>Frontend App Placeholder</p>
  </div>
);

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
);

console.log('Frontend app main.tsx loaded');
